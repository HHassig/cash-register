require "condense_baskets"

RSpec.describe CondenseBaskets do
  describe ".condense" do
    it 'Should return an array of hashes with uniquie item_ids' do
      ## Where can I create and load my test array
      expect(2).to eq 1 + 1
    end
  end
end
